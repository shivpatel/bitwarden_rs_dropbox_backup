version: '3.1'
services:
  bitwarden_dropbox_backup:
    build: .
    restart: always
    volumes:
      - ./db.sqlite3:/db.sqlite3
    environment:
      - DROPBOX_ACCESS_TOKEN=xxx
      - BACKUP_ENCRYPTION_KEY=xxx